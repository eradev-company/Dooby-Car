import requests
import json
import threading
import time
from functions import *


if __name__ == "__main__":
    # while True:
    #     print("this will run after every 2 sec")
    #     update_info()
    #     time.sleep(5)
    blockState()